 a script that connects to MongoDB using process.env.MONGODB_URI, ensures a TTL index on expiresAt (sparse, expireAfterSeconds: 0), deletes any existing doc with slug 'qa-smoke-test', reads qa-test-course.json from the same directory, sets expiresAt to Date.now() + 60 minutes, removes the _qa key, inserts it using the InteractiveCourse model (require the model from './models/InteractiveCourse'), logs the course title/slug/id/expiration, then disconnects. Keep it simple — no analytics verification, just seed and confirm.

For the course JSON: use this structure — title: "QA Smoke Test – Platform Verification", slug: "qa-smoke-test", ceHours: 0.5, ceProvider: "NBCC ACEP #7760", acepNumber: "7760", status: "draft", isTestCourse: true, 3 sections with these block types spread across them: sectionDivider, text, imageText, accordion, matching, flashcardDeck, multipleChoice, multiSelect, sequencing, reflection, resources, references. Keep content minimal — just enough to verify each block renders. Include a 5-question assessment (3 multipleChoice, 1 multiSelect, 1 trueFalse) with passThreshold 0.7 and attemptsAllowed 99. Add an analytics object with flags: trackEnrollment, trackSectionProgress, trackBlockInteractions, trackAssessmentAttempts, trackCertificateGeneration, trackTimerEvents — all true.
Do not modify any existing files.
